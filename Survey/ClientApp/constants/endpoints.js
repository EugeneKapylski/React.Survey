import urls from '../config/urls';

const surveys = `${urls.SERVICE_BASE_URL}/surveys`;
const surveyPassings = `${urls.SERVICE_BASE_URL}/surveyPassings`;

export default {
    surveys,
    surveyPassings
};