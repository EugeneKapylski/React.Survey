import React from 'react';

import './header-title.scss';

const HeaderTitle = () => <h1 className="header-title">Survey Application</h1>;

HeaderTitle.displayName = 'HeaderTitle';

export default HeaderTitle;